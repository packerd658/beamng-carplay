{
  "name": "beamplay-carplay-mod",
  "version": "1.0.0",
  "private": true,
  "description": "Test harness for the BeamPlay BeamNG.drive UI app (a CarPlay-inspired in-car display).",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
